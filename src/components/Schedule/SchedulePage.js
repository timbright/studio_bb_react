import React from "react";
import classes from "./SchedulePage.module.scss";

const SchedulePage = () => (
	<section className={classes.SchedulePage}>
		<h1 className="visually-hidden">Schedule</h1>
	</section>
);

export default SchedulePage;
