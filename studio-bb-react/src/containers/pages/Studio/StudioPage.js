import React from "react";
import classes from "./StudioPage.module.scss";

const StudioPage = () => (
	<section className={classes.StudioPage}>
		<h1 className="visually-hidden">Studio</h1>
	</section>
);

export default StudioPage;
